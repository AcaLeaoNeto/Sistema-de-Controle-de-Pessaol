using Domain.Entitys.Login;
using FluentValidation;
using System.Data;

namespace Presentation.Validation
{
    public class LogValidation //: AbstractValidator<SingOn>
    {
        //public LogValidation()
        //{
        //    RuleFor(l => l.Password).NotEmpty().Must(l => (l.Any(c => char.IsDigit(c)))).WithMessage("Precisa ser numero");
        //}
    }
}
